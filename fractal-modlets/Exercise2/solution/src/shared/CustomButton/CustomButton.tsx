import styles from "./CustomButton.module.css"

const CustomButton = () =>{
  return(
    <button>
      I'm a custom button! I'm shared across the whole codebase!
    </button>
  )
}

export default CustomButton