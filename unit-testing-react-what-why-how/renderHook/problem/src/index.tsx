// useLoggedInUser will always return the same, but for simplicity sake, pretend it’s a good hook.
export default function useLoggedInUser() {
	return { name: "Alice", email: "envkt@example.com" };
}
